#include <stdio.h>

/**
 * main - entry point
 * Rev
 * Return: 0 (Success)
 */

int main(void)
{
	char ch;
	int i;

	for (i = 0; i <= 0; i++)
	{
		putchar(i + 48);
	}
	for (ch = 'a'; ch <= 'f'; ch++)
	{
		putchar(ch);
	}
	putchar('\n');
	return (0);
}
